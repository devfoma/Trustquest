import "../globals.css"

export const metadata = {
  title: "TrustQuest - DApp",
  description: "Save & win with no-loss prize savings on Stellar",
}

export default function AppLayout({ children }) {
  return <div className="dark">{children}</div>
}


